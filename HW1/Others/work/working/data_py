import csv
import os
import sys
import json
import time
# Allowed Library
import numpy as np

beginning_t = time.time()
print(time.ctime())

with open('train.csv', 'r', encoding='big5', newline='') as csvff:   
    data = np.array([[eval(data) if data != 'NR' else 0 for data in r[3:]] \
    for r in ([r for r in csv.reader(csvff)][1:])]).reshape(12, 20, 18, 24) \
    .transpose(0, 1, 3, 2).reshape(12, -1, 18)
pm25 = data.T[9].T